/**
 * @brief function to concatenate 2 dynamic strings, the concatenated strings starts with s1 and ends with s2, followed by \0.
 *        The freeing of memory it's up to the caller.
 * @params char *s1, the first string
 *         char *s2, the second string
 * @return char * -> the string resulting of the concatenation process.
*/
char *sumStrings(char *s1, char *s2);
